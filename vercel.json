{
  "framework": null,
  "buildCommand": "echo 'No build step needed -- Vercel bundles api/*.ts directly.'",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
